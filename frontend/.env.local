# Local development overrides — NOT committed to git
# Points the frontend at your local API server so dev builds
# don't hit the live site and trigger CORS errors.
NEXT_PUBLIC_API_URL=http://localhost:8000
